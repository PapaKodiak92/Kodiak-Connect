# Kodiak Connect v1.1.2 — Message Actions Patch

## Fixed
- Fixed message action access so right-click opens message actions more reliably.
- Added a visible message action button on hover/focus as a fallback for edit, reply, react, and delete.
- Moved the top-right profile pill down so it no longer sits too high against the titlebar.

## Notes
- This patch makes it easier for admins to delete duplicate dev update posts from inside Kodiak Connect.
